# Habilitar el motor de reescritura
RewriteEngine On

# Permitir acceso directo a archivos PHP
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule \.php$ - [L]

# Permitir acceso a archivos CSS, JS e imágenes
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule \.(css|js|png|jpg|jpeg|gif|ico|svg)$ - [L]

# No hacer nada más - dejar que Apache maneje el resto normalmente
